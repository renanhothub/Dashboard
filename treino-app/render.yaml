# Deploy no Render (https://render.com): New > Blueprint > selecione este repositório.
services:
  - type: web
    name: treino-pro
    runtime: docker
    rootDir: treino-app
    plan: starter
    healthCheckPath: /api/health
    disk:
      name: dados
      mountPath: /data
      sizeGB: 1
